{
  "name": "cybertrust-ms",
  "module": "index.ts",
  "type": "commonjs",
  "private": true,
  "scripts": {
    "dev": "bun --filter '*' dev",
    "start": "bun --filter '*' start",
    "build": "bun --filter '*' build"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": [
    "packages/*"
  ]
}
